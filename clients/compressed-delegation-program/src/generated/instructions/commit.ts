/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from '@solana/kit';
import { COMPRESSED_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getCommitArgsDecoder,
  getCommitArgsEncoder,
  type CommitArgs,
  type CommitArgsArgs,
} from '../types';

export const COMMIT_DISCRIMINATOR = 2;

export function getCommitDiscriminatorBytes() {
  return getU64Encoder().encode(COMMIT_DISCRIMINATOR);
}

export type CommitInstruction<
  TProgram extends string = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
  TAccountValidator extends string | AccountMeta<string> = string,
  TAccountDelegatedAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountValidator extends string
        ? WritableSignerAccount<TAccountValidator> &
            AccountSignerMeta<TAccountValidator>
        : TAccountValidator,
      TAccountDelegatedAccount extends string
        ? ReadonlyAccount<TAccountDelegatedAccount>
        : TAccountDelegatedAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CommitInstructionData = { discriminator: bigint; args: CommitArgs };

export type CommitInstructionDataArgs = { args: CommitArgsArgs };

export function getCommitInstructionDataEncoder(): Encoder<CommitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU64Encoder()],
      ['args', getCommitArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: COMMIT_DISCRIMINATOR })
  );
}

export function getCommitInstructionDataDecoder(): Decoder<CommitInstructionData> {
  return getStructDecoder([
    ['discriminator', getU64Decoder()],
    ['args', getCommitArgsDecoder()],
  ]);
}

export function getCommitInstructionDataCodec(): Codec<
  CommitInstructionDataArgs,
  CommitInstructionData
> {
  return combineCodec(
    getCommitInstructionDataEncoder(),
    getCommitInstructionDataDecoder()
  );
}

export type CommitInput<
  TAccountValidator extends string = string,
  TAccountDelegatedAccount extends string = string,
> = {
  validator: TransactionSigner<TAccountValidator>;
  delegatedAccount: Address<TAccountDelegatedAccount>;
  args: CommitInstructionDataArgs['args'];
};

export function getCommitInstruction<
  TAccountValidator extends string,
  TAccountDelegatedAccount extends string,
  TProgramAddress extends
    Address = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
>(
  input: CommitInput<TAccountValidator, TAccountDelegatedAccount>,
  config?: { programAddress?: TProgramAddress }
): CommitInstruction<
  TProgramAddress,
  TAccountValidator,
  TAccountDelegatedAccount
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPRESSED_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    validator: { value: input.validator ?? null, isWritable: true },
    delegatedAccount: {
      value: input.delegatedAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.validator),
      getAccountMeta(accounts.delegatedAccount),
    ],
    data: getCommitInstructionDataEncoder().encode(
      args as CommitInstructionDataArgs
    ),
    programAddress,
  } as CommitInstruction<
    TProgramAddress,
    TAccountValidator,
    TAccountDelegatedAccount
  >);
}

export type ParsedCommitInstruction<
  TProgram extends string = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    validator: TAccountMetas[0];
    delegatedAccount: TAccountMetas[1];
  };
  data: CommitInstructionData;
};

export function parseCommitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCommitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      validator: getNextAccount(),
      delegatedAccount: getNextAccount(),
    },
    data: getCommitInstructionDataDecoder().decode(instruction.data),
  };
}
