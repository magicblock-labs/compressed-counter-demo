/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { COMPRESSED_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getUndelegateArgsDecoder,
  getUndelegateArgsEncoder,
  type UndelegateArgs,
  type UndelegateArgsArgs,
} from '../types';

export const UNDELEGATE_DISCRIMINATOR = 4;

export function getUndelegateDiscriminatorBytes() {
  return getU64Encoder().encode(UNDELEGATE_DISCRIMINATOR);
}

export type UndelegateInstruction<
  TProgram extends string = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountDelegatedAccount extends string | AccountMeta<string> = string,
  TAccountOwnerProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountDelegatedAccount extends string
        ? WritableAccount<TAccountDelegatedAccount>
        : TAccountDelegatedAccount,
      TAccountOwnerProgram extends string
        ? ReadonlyAccount<TAccountOwnerProgram>
        : TAccountOwnerProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UndelegateInstructionData = {
  discriminator: bigint;
  args: UndelegateArgs;
};

export type UndelegateInstructionDataArgs = { args: UndelegateArgsArgs };

export function getUndelegateInstructionDataEncoder(): Encoder<UndelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU64Encoder()],
      ['args', getUndelegateArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UNDELEGATE_DISCRIMINATOR })
  );
}

export function getUndelegateInstructionDataDecoder(): Decoder<UndelegateInstructionData> {
  return getStructDecoder([
    ['discriminator', getU64Decoder()],
    ['args', getUndelegateArgsDecoder()],
  ]);
}

export function getUndelegateInstructionDataCodec(): Codec<
  UndelegateInstructionDataArgs,
  UndelegateInstructionData
> {
  return combineCodec(
    getUndelegateInstructionDataEncoder(),
    getUndelegateInstructionDataDecoder()
  );
}

export type UndelegateInput<
  TAccountPayer extends string = string,
  TAccountDelegatedAccount extends string = string,
  TAccountOwnerProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  delegatedAccount: Address<TAccountDelegatedAccount>;
  ownerProgram: Address<TAccountOwnerProgram>;
  systemProgram: Address<TAccountSystemProgram>;
  args: UndelegateInstructionDataArgs['args'];
};

export function getUndelegateInstruction<
  TAccountPayer extends string,
  TAccountDelegatedAccount extends string,
  TAccountOwnerProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends
    Address = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
>(
  input: UndelegateInput<
    TAccountPayer,
    TAccountDelegatedAccount,
    TAccountOwnerProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UndelegateInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountDelegatedAccount,
  TAccountOwnerProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPRESSED_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    delegatedAccount: {
      value: input.delegatedAccount ?? null,
      isWritable: true,
    },
    ownerProgram: { value: input.ownerProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.delegatedAccount),
      getAccountMeta(accounts.ownerProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUndelegateInstructionDataEncoder().encode(
      args as UndelegateInstructionDataArgs
    ),
    programAddress,
  } as UndelegateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDelegatedAccount,
    TAccountOwnerProgram,
    TAccountSystemProgram
  >);
}

export type ParsedUndelegateInstruction<
  TProgram extends string = typeof COMPRESSED_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    delegatedAccount: TAccountMetas[1];
    ownerProgram: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: UndelegateInstructionData;
};

export function parseUndelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUndelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      delegatedAccount: getNextAccount(),
      ownerProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUndelegateInstructionDataDecoder().decode(instruction.data),
  };
}
