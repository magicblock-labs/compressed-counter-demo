/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TEST_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getIncrementCompressedCounterArgsDecoder,
  getIncrementCompressedCounterArgsEncoder,
  type IncrementCompressedCounterArgs,
  type IncrementCompressedCounterArgsArgs,
} from '../types';

export const INCREMENT_COMPRESSED_COUNTER_DISCRIMINATOR = 4;

export function getIncrementCompressedCounterDiscriminatorBytes() {
  return getU64Encoder().encode(INCREMENT_COMPRESSED_COUNTER_DISCRIMINATOR);
}

export type IncrementCompressedCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCounter extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      ...TRemainingAccounts,
    ]
  >;

export type IncrementCompressedCounterInstructionData = {
  discriminator: bigint;
  args: IncrementCompressedCounterArgs;
};

export type IncrementCompressedCounterInstructionDataArgs = {
  args: IncrementCompressedCounterArgsArgs;
};

export function getIncrementCompressedCounterInstructionDataEncoder(): Encoder<IncrementCompressedCounterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU64Encoder()],
      ['args', getIncrementCompressedCounterArgsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INCREMENT_COMPRESSED_COUNTER_DISCRIMINATOR,
    })
  );
}

export function getIncrementCompressedCounterInstructionDataDecoder(): Decoder<IncrementCompressedCounterInstructionData> {
  return getStructDecoder([
    ['discriminator', getU64Decoder()],
    ['args', getIncrementCompressedCounterArgsDecoder()],
  ]);
}

export function getIncrementCompressedCounterInstructionDataCodec(): Codec<
  IncrementCompressedCounterInstructionDataArgs,
  IncrementCompressedCounterInstructionData
> {
  return combineCodec(
    getIncrementCompressedCounterInstructionDataEncoder(),
    getIncrementCompressedCounterInstructionDataDecoder()
  );
}

export type IncrementCompressedCounterInput<
  TAccountPayer extends string = string,
  TAccountCounter extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  counter: Address<TAccountCounter>;
  args: IncrementCompressedCounterInstructionDataArgs['args'];
};

export function getIncrementCompressedCounterInstruction<
  TAccountPayer extends string,
  TAccountCounter extends string,
  TProgramAddress extends Address = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
>(
  input: IncrementCompressedCounterInput<TAccountPayer, TAccountCounter>,
  config?: { programAddress?: TProgramAddress }
): IncrementCompressedCounterInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountCounter
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEST_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    counter: { value: input.counter ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.counter),
    ],
    data: getIncrementCompressedCounterInstructionDataEncoder().encode(
      args as IncrementCompressedCounterInstructionDataArgs
    ),
    programAddress,
  } as IncrementCompressedCounterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCounter
  >);
}

export type ParsedIncrementCompressedCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    counter: TAccountMetas[1];
  };
  data: IncrementCompressedCounterInstructionData;
};

export function parseIncrementCompressedCounterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedIncrementCompressedCounterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount(), counter: getNextAccount() },
    data: getIncrementCompressedCounterInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
