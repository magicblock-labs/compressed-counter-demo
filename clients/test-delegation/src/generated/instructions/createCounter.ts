/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TEST_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CREATE_COUNTER_DISCRIMINATOR = 0;

export function getCreateCounterDiscriminatorBytes() {
  return getU64Encoder().encode(CREATE_COUNTER_DISCRIMINATOR);
}

export type CreateCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateCounterInstructionData = { discriminator: bigint };

export type CreateCounterInstructionDataArgs = {};

export function getCreateCounterInstructionDataEncoder(): FixedSizeEncoder<CreateCounterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU64Encoder()]]),
    (value) => ({ ...value, discriminator: CREATE_COUNTER_DISCRIMINATOR })
  );
}

export function getCreateCounterInstructionDataDecoder(): FixedSizeDecoder<CreateCounterInstructionData> {
  return getStructDecoder([['discriminator', getU64Decoder()]]);
}

export function getCreateCounterInstructionDataCodec(): FixedSizeCodec<
  CreateCounterInstructionDataArgs,
  CreateCounterInstructionData
> {
  return combineCodec(
    getCreateCounterInstructionDataEncoder(),
    getCreateCounterInstructionDataDecoder()
  );
}

export type CreateCounterInput<
  TAccountPayer extends string = string,
  TAccountCounter extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  counter: Address<TAccountCounter>;
  systemProgram: Address<TAccountSystemProgram>;
};

export function getCreateCounterInstruction<
  TAccountPayer extends string,
  TAccountCounter extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
>(
  input: CreateCounterInput<
    TAccountPayer,
    TAccountCounter,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateCounterInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountCounter,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEST_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    counter: { value: input.counter ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateCounterInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateCounterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCounter,
    TAccountSystemProgram
  >);
}

export type ParsedCreateCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    counter: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateCounterInstructionData;
};

export function parseCreateCounterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateCounterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      counter: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateCounterInstructionDataDecoder().decode(instruction.data),
  };
}
