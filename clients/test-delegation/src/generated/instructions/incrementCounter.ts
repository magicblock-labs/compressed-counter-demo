/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { TEST_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INCREMENT_COUNTER_DISCRIMINATOR = 1;

export function getIncrementCounterDiscriminatorBytes() {
  return getU64Encoder().encode(INCREMENT_COUNTER_DISCRIMINATOR);
}

export type IncrementCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type IncrementCounterInstructionData = { discriminator: bigint };

export type IncrementCounterInstructionDataArgs = {};

export function getIncrementCounterInstructionDataEncoder(): FixedSizeEncoder<IncrementCounterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU64Encoder()]]),
    (value) => ({ ...value, discriminator: INCREMENT_COUNTER_DISCRIMINATOR })
  );
}

export function getIncrementCounterInstructionDataDecoder(): FixedSizeDecoder<IncrementCounterInstructionData> {
  return getStructDecoder([['discriminator', getU64Decoder()]]);
}

export function getIncrementCounterInstructionDataCodec(): FixedSizeCodec<
  IncrementCounterInstructionDataArgs,
  IncrementCounterInstructionData
> {
  return combineCodec(
    getIncrementCounterInstructionDataEncoder(),
    getIncrementCounterInstructionDataDecoder()
  );
}

export type IncrementCounterInput<
  TAccountCounter extends string = string,
  TAccountAuthority extends string = string,
> = {
  counter: Address<TAccountCounter>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getIncrementCounterInstruction<
  TAccountCounter extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
>(
  input: IncrementCounterInput<TAccountCounter, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): IncrementCounterInstruction<
  TProgramAddress,
  TAccountCounter,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEST_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    counter: { value: input.counter ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.authority),
    ],
    data: getIncrementCounterInstructionDataEncoder().encode({}),
    programAddress,
  } as IncrementCounterInstruction<
    TProgramAddress,
    TAccountCounter,
    TAccountAuthority
  >);
}

export type ParsedIncrementCounterInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    counter: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: IncrementCounterInstructionData;
};

export function parseIncrementCounterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedIncrementCounterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { counter: getNextAccount(), authority: getNextAccount() },
    data: getIncrementCounterInstructionDataDecoder().decode(instruction.data),
  };
}
