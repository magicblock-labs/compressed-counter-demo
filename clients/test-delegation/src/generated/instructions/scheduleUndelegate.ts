/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TEST_DELEGATION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SCHEDULE_UNDELEGATE_DISCRIMINATOR = 7;

export function getScheduleUndelegateDiscriminatorBytes() {
  return getU64Encoder().encode(SCHEDULE_UNDELEGATE_DISCRIMINATOR);
}

export type ScheduleUndelegateInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountMagicContext extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ScheduleUndelegateInstructionData = { discriminator: bigint };

export type ScheduleUndelegateInstructionDataArgs = {};

export function getScheduleUndelegateInstructionDataEncoder(): FixedSizeEncoder<ScheduleUndelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU64Encoder()]]),
    (value) => ({ ...value, discriminator: SCHEDULE_UNDELEGATE_DISCRIMINATOR })
  );
}

export function getScheduleUndelegateInstructionDataDecoder(): FixedSizeDecoder<ScheduleUndelegateInstructionData> {
  return getStructDecoder([['discriminator', getU64Decoder()]]);
}

export function getScheduleUndelegateInstructionDataCodec(): FixedSizeCodec<
  ScheduleUndelegateInstructionDataArgs,
  ScheduleUndelegateInstructionData
> {
  return combineCodec(
    getScheduleUndelegateInstructionDataEncoder(),
    getScheduleUndelegateInstructionDataDecoder()
  );
}

export type ScheduleUndelegateInput<
  TAccountPayer extends string = string,
  TAccountCounter extends string = string,
  TAccountMagicContext extends string = string,
  TAccountMagicProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  counter: Address<TAccountCounter>;
  magicContext: Address<TAccountMagicContext>;
  magicProgram: Address<TAccountMagicProgram>;
};

export function getScheduleUndelegateInstruction<
  TAccountPayer extends string,
  TAccountCounter extends string,
  TAccountMagicContext extends string,
  TAccountMagicProgram extends string,
  TProgramAddress extends Address = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
>(
  input: ScheduleUndelegateInput<
    TAccountPayer,
    TAccountCounter,
    TAccountMagicContext,
    TAccountMagicProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ScheduleUndelegateInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountCounter,
  TAccountMagicContext,
  TAccountMagicProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEST_DELEGATION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    counter: { value: input.counter ?? null, isWritable: true },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.magicContext),
      getAccountMeta(accounts.magicProgram),
    ],
    data: getScheduleUndelegateInstructionDataEncoder().encode({}),
    programAddress,
  } as ScheduleUndelegateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCounter,
    TAccountMagicContext,
    TAccountMagicProgram
  >);
}

export type ParsedScheduleUndelegateInstruction<
  TProgram extends string = typeof TEST_DELEGATION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    counter: TAccountMetas[1];
    magicContext: TAccountMetas[2];
    magicProgram: TAccountMetas[3];
  };
  data: ScheduleUndelegateInstructionData;
};

export function parseScheduleUndelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedScheduleUndelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      counter: getNextAccount(),
      magicContext: getNextAccount(),
      magicProgram: getNextAccount(),
    },
    data: getScheduleUndelegateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
